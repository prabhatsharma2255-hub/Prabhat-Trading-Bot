services:
  - type: web
    name: trading-bot
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: FORCE_DRY_RUN
        value: "true"